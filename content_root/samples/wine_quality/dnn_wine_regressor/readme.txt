This example consists of a simple TesorFlow DNN and LR models trained a 70% training split of the UCI wine
quality dataset (similar to SciKit example).

package.py implements predictions using DNN model.
